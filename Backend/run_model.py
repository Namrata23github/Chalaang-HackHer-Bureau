import joblib
# Load the model
# model = joblib.load('model.pkl')

def make_prediction():
    # Prepare the data
   # data = input_data

    # Make a prediction
   # prediction = model.predict([input_data.array(list(data.values()))])
    prediction = 0
    return prediction